<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
android:layout_width="match_parent"
android:layout_height="match_parent"
tools:context=".ui.playeditor.PlayEditorFragment">

<Button
    android:id="@+id/back_button"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:background="@drawable/custom_radio_emotion"
    android:button="@android:color/transparent"
    android:text="@string/play_overview_bttn"
    app:layout_constraintBottom_toTopOf="@+id/scene_spinner"
    app:layout_constraintHeight_percent=".06"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent" />

    <Spinner
        android:id="@+id/scene_spinner"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/playline_view"
        app:layout_constraintHeight_percent=".06"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/back_button"
        />


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/playline_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/lay"
        app:layout_constraintHeight_percent=".75"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/scene_spinner"
        />

    <LinearLayout
        android:id="@+id/lay"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHeight_percent=".13"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/playline_view">


        <Button
            android:id="@+id/selec_macros"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/custom_radio_emotion"
            android:button="@android:color/transparent"
            android:paddingBottom="10dp"
            android:text="@string/selec_macros" />

        <Button
            android:id="@+id/play_button"
            android:paddingTop="10dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/custom_radio_emotion"
            android:button="@android:color/transparent"
            android:text="@string/next_scene" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>